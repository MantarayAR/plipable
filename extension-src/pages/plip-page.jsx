var React = require('react');
var ReactDOM = require('react-dom');

var PlipPage = function(Asteroid) {
  var react = function() {
    return (
      <div>
        Test
      </div>
    );
  };

  return {
    react: react
  };
};

module.exports = PlipPage;